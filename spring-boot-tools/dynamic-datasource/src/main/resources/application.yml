server:
  port: 1010
spring:
  profiles:
    active: ${PROFILE_ACTIVE:dev}
  application:
    name: dynamic-datasource-application

mybatis:
  config-location: classpath:mybatis/config/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml